get:
  tags: [user]
  description: |
    Gets user info.
  summary: Get user info.
  operationId: api.me.search
  responses:
    "200":
      description: The user informations
      content:
        application/json:
          schema:
            $ref: "../components/schemas.yaml#/User"
    "400":
      $ref: "../components/responses.yaml#/BadRequest"