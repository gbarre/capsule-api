version: "3"

services:
  db:
    image: mariadb:10.1
    env_file:
      - .env.local
    ports:
      - '127.0.0.1:30306:3306'
    volumes:
      - db_data:/var/lib/mysql

volumes:
  db_data:
    driver: local